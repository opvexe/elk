input {
	tcp {
		port => 5000
	}

	file {
            path => "/Users/facebook/go/src/shumin-project/elk-server/home/logs/*.log"
            start_position => beginning
        }
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "elastic" # 用户名
		password => "changeme" #密码
	}
}
